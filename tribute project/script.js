
document.getElementById('scrollButton').addEventListener('click', function() {
    // Scroll down 500 pixels (adjust as needed)
    window.scrollBy({
        top: 500,
        behavior: 'smooth' // Use smooth scrolling
    });
});